/// <reference types="react" />
import { IntentParameters } from './types';
/**
 *
 * @example
 * ```tsx
 * import {useIntentLink} from 'sanity'
 *
 * const {onClick, href} = useIntentLink({
 *   intent: 'edit',
 *   params: {id: 'foo'}
 * })
 *
 * <a href={href} onClick={onClick}>Link to "foo" editor</a>
 * ```
 *
 * @public
 */
export declare function useIntentLink(props: {
    intent: string;
    onClick?: React.MouseEventHandler<HTMLElement>;
    params?: IntentParameters;
    replace?: boolean;
    target?: string;
}): {
    onClick: React.MouseEventHandler<HTMLElement>;
    href: string;
};
//# sourceMappingURL=useIntentLink.d.ts.map