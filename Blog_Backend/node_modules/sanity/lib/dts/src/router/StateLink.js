import React, { forwardRef } from 'react';
import { useStateLink } from './useStateLink';
/**
 * @public
 */
export const StateLink = forwardRef(function StateLink(props, ref) {
    const { onClick: onClickProp, replace, state, target, toIndex = false, ...restProps } = props;
    const { onClick, href } = useStateLink({
        onClick: onClickProp,
        replace,
        state,
        target,
        toIndex,
    });
    return React.createElement("a", { ...restProps, href: href, onClick: onClick, ref: ref });
});
//# sourceMappingURL=StateLink.js.map