import React, { forwardRef } from 'react';
import { useIntentLink } from './useIntentLink';
/**
 * @public
 */
export const IntentLink = forwardRef(function IntentLink(props, ref) {
    const { intent, params, target, ...restProps } = props;
    const { onClick, href } = useIntentLink({
        intent,
        params,
        target,
        onClick: props.onClick,
    });
    return React.createElement("a", { ...restProps, href: href, onClick: onClick, ref: ref, target: target });
});
//# sourceMappingURL=IntentLink.js.map