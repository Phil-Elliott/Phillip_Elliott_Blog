import React from 'react';
import { TextArea } from '@sanity/ui';
import styled from 'styled-components';
const StyledTextArea = styled(TextArea) `
  &[data-as='textarea'] {
    resize: vertical;
  }
`;
export function TextInput(props) {
    const { schemaType, validationError, value, elementProps } = props;
    return (React.createElement(StyledTextArea, { customValidity: validationError, value: value || '', placeholder: schemaType.placeholder, rows: typeof schemaType.rows === 'number' ? schemaType.rows : 10, ...elementProps }));
}
//# sourceMappingURL=TextInput.js.map