import { TextArea } from '@sanity/ui';
import React, { useCallback } from 'react';
import styled, { css } from 'styled-components';
import { set } from '../../../../patch';
const DebugTextArea = styled(TextArea)(({ theme }) => {
    return css `
    font-family: ${theme.sanity.fonts.code.family};
  `;
});
function DebugInput(props) {
    const { onChange, readOnly, elementProps } = props;
    const handleChange = useCallback(() => {
        onChange(set({}));
    }, [onChange]);
    return (React.createElement(DebugTextArea, { ...elementProps, onChange: handleChange, padding: 3, radius: 1, readOnly: readOnly, rows: 100, value: JSON.stringify(props.value, null, 2) }));
}
// export const resolveInputComponent: FormInputComponentResolver = () => {
//   return DebugInput
// }
// export const resolvePreviewComponent = () => {
//   return function PreviewAny() {
//     return <div>preview</div>
//   }
// }
//# sourceMappingURL=formBuilder.js.map