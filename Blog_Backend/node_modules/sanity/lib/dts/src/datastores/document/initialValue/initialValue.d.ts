import { InitialValueResolverContext, Schema } from '@sanity/types';
import { Observable } from 'rxjs';
import { DocumentPreviewStore } from '../../../preview';
import { Template } from '../../../templates';
import { InitialValueMsg } from './types';
export interface InitialValueOptions {
    documentId: string;
    documentType: string;
    templateName?: string;
    templateParams?: Record<string, any>;
}
/**
 * @internal
 */
export declare function getInitialValueStream(schema: Schema, initialValueTemplates: Template[], documentPreviewStore: DocumentPreviewStore, opts: InitialValueOptions, context: InitialValueResolverContext): Observable<InitialValueMsg>;
//# sourceMappingURL=initialValue.d.ts.map