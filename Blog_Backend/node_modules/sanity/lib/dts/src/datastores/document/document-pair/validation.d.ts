import type { SanityClient } from '@sanity/client';
import { Observable } from 'rxjs';
import { ValidationMarker, Schema } from '@sanity/types';
import { SourceClientOptions } from '../../../config';
import { IdPair } from '../types';
import { HistoryStore } from '../../history';
import { DocumentPreviewStore } from '../../../preview';
export interface ValidationStatus {
    isValidating: boolean;
    validation: ValidationMarker[];
}
export declare const validation: (arg1: {
    client: SanityClient;
    getClient: (options: SourceClientOptions) => SanityClient;
    documentPreviewStore: DocumentPreviewStore;
    historyStore: HistoryStore;
    schema: Schema;
}, arg2: IdPair, arg3: string) => Observable<ValidationStatus>;
//# sourceMappingURL=validation.d.ts.map