import { useCallback } from 'react';
import { resolveInitialValueForType } from '../../templates';
import { DEFAULT_MAX_RECURSION_DEPTH } from '../../templates/resolve';
import { useInitialValueResolverContext } from './useInitialValue';
export function useResolveInitialValueForType() {
    const initialValueContext = useInitialValueResolverContext();
    return useCallback((type, params) => {
        return resolveInitialValueForType(type, params, DEFAULT_MAX_RECURSION_DEPTH, initialValueContext);
    }, [initialValueContext]);
}
//# sourceMappingURL=useResolveInitialValueForType.js.map