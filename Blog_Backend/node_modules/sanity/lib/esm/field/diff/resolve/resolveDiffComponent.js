import { diffResolvers } from '../../TODO';
import { defaultComponents } from './defaultComponents';
export function resolveDiffComponent(type, parentSchemaType) {
  let itType = type;

  while (itType) {
    var _itType, _itType$components;

    const resolved = ((_itType = itType) === null || _itType === void 0 ? void 0 : (_itType$components = _itType.components) === null || _itType$components === void 0 ? void 0 : _itType$components.diff) || tryResolve(itType, parentSchemaType) || defaultComponents[itType.name];

    if (resolved) {
      return resolved;
    }

    itType = itType.type;
  }

  return defaultComponents[type.jsonType];
}

function tryResolve(schemaType, parentSchemaType) {
  const resolvers = diffResolvers;
  let resolved;

  for (const resolver of resolvers) {
    if (typeof resolver !== 'function') {
      // eslint-disable-next-line no-console
      console.error('Diff component resolver is not a function: ', resolver);
      continue;
    }

    resolved = resolver({
      schemaType,
      parentSchemaType
    });

    if (resolved) {
      return resolved;
    }
  }

  return undefined;
}