function test(preview) {
    return preview;
}
const t = test({
    select: {
        title: 'title',
        subtitle: 'subtitle',
    },
    prepare({ title }) {
        return {};
    },
});
export {};
//# sourceMappingURL=preview.js.map