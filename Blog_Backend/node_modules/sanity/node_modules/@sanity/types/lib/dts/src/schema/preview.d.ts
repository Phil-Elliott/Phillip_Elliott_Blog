import type { ReactNode } from 'react';
import type { SortOrdering } from './types';
export interface PrepareViewOptions {
    ordering?: SortOrdering;
}
export interface PreviewValue {
    title?: ReactNode;
    subtitle?: ReactNode;
    description?: ReactNode;
    media?: ReactNode;
    imageUrl?: string;
}
export interface PreviewConfig<Select extends Record<string, string> = Record<string, string>, PrepareValue extends Record<keyof Select, any> = Record<keyof Select, any>> {
    select?: Select;
    prepare?: (value: PrepareValue, viewOptions?: PrepareViewOptions) => PreviewValue;
}
//# sourceMappingURL=preview.d.ts.map